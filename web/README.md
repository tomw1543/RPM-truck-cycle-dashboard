# HaulCycle Insights: web

React frontend (Vite, TypeScript, Tailwind CSS, TanStack Query, React Router,
Recharts). See the root [`README.md`](../README.md#web) for setup.

```sh
npm install
npm run dev      # http://localhost:5173, requires the API running on :5281
npm run build
npm run lint
```
